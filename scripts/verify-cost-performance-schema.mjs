import assert from 'node:assert/strict';import{readFileSync}from'node:fs';const s=JSON.parse(readFileSync(new URL('../packages/contracts/schemas/cost-performance-telemetry.v1.json',import.meta.url),'utf8'));assert.equal(s.properties.authority.const,'telemetry-only');assert.ok(s.$defs.usage.required.includes('wallDurationMs'));for(const k of ['inputTokens','outputTokens','gpuDurationMs','mediaDurationMs'])assert.ok(s.$defs.usage.properties[k]);assert.ok(s.$defs.cost.required.includes('pricingVersion'));console.log('PASS: cost/performance telemetry JSON Schema authority markers verified');
