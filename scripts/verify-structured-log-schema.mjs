import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';const schema=JSON.parse(await readFile(new URL('../packages/contracts/schemas/ai-editor-structured-log.v1.json',import.meta.url),'utf8'));assert.equal(schema.additionalProperties,false);assert.equal(schema.properties.schemaVersion.const,'1.0');assert.equal(schema.properties.correlation.additionalProperties,false);assert.equal(schema.properties.versions.additionalProperties,false);assert.equal(schema.properties.attempt.minimum,1);for(const forbidden of ['rawPrompt','transcript','ocrText','mediaPath','sourceUrl','chainOfThought'])assert.equal(JSON.stringify(schema).includes(forbidden),false);console.log('PASS: structured log JSON Schema authority markers verified');
